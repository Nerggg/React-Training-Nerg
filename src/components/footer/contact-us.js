import React from 'react';
import './FooterRect.css'; // import the CSS file that styles the rectangle

const ContactUs = () => {
  return (
    <div className="contact-us">
      <span className="contact-us-word">Contact US!</span>
    </div>
  );
};

export default ContactUs;
